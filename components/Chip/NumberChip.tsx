const NumberChip = ({ count }: { count: number }) => {
  return <div className='w-5 h-5 py-[1px] text-xs font-medium text-center bg-tp-gray_600 rounded  '>{count}</div>;
};

export default NumberChip;
